import styled from "styled-components";

export const HttpVerb = styled.div`
  background: ${props => props.color};
  border-radius: 3px;
  display: inline-block;
  padding: 3px;
  align-items: center;
`;